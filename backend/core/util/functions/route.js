function route(name){
  const host =  '';
  const path = '';

  const url = host + path;
  return url;
}

config('server.protocol', 'http')
config('server.host', '127.0.0.1')
config('server.port', '127.0.0.1')